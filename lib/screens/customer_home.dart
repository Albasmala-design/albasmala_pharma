import 'package:flutter/material.dart';

class CustomerHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('لوحة الزبون')),
      body: Center(child: Text('مرحباً في البسمله الطبيه! 🎉')),
    );
  }
}
