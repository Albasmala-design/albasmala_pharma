import 'package:flutter/material.dart';
class OwnerHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('لوحة المالك')), body: Center(child: Text('لوحة المالك')));
  }
}
